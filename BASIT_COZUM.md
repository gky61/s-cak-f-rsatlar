# 🚀 EN BASIT ÇÖZÜM - Firebase Login

## ⚠️ ÖNEMLİ: Script'i Çalıştırmadan Önce Proje Klasörüne Gidin!

---

## ✅ ADIM ADIM (Kopyala-Yapıştır)

Terminal'de **SIRAYLA** şu komutları çalıştırın (her birini Enter'a basarak):

### 1. Proje Klasörüne Git
```bash
cd "/Users/gokayalemdar/Desktop/SICAK FIRSATLAR"
```

### 2. Node.js 20'ye Geç
```bash
export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" && nvm use 20
```

### 3. Firebase Login Çalıştır
```bash
./firebase_login.sh
```

---

## 📋 VEYA MANUEL OLARAK (Daha Kontrollü)

### Adım 1: Proje Klasörüne Git
```bash
cd "/Users/gokayalemdar/Desktop/SICAK FIRSATLAR"
```

### Adım 2: NVM'i Yükle
```bash
export NVM_DIR="$HOME/.nvm"
```

### Adım 3: NVM'i Aktif Et
```bash
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
```

### Adım 4: Node.js 20'ye Geç
```bash
nvm use 20
```

### Adım 5: Firebase Login
```bash
firebase login
```

Sonra terminal'de görünen URL'yi tarayıcıda açın ve giriş yapın.

---

## 🔍 Kontrol: Neredeysiniz?

Terminal'de şu komutu çalıştırın:

```bash
pwd
```

**Görmeniz gereken:**
```
/Users/gokayalemdar/Desktop/SICAK FIRSATLAR
```

Eğer farklı bir dizindeyse, yukarıdaki `cd` komutunu çalıştırın.






